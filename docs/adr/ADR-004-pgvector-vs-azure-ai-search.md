# ADR-004: pgvector versus Azure AI Search

pgvector is lower-friction for local demos and transactional metadata. Azure AI Search adds managed hybrid retrieval, filtering and operational scaling. The application owns a retriever port so the use case is vendor-neutral.
