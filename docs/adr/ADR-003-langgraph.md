# ADR-003: LangGraph for explicit workflow state

The graph makes retrieval, retry, answer and validation states visible. Retrieval/answer/validation are specialized roles, not unnecessary autonomous processes.
